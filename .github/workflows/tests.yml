name: Run Tests

on:
  workflow_call:
  workflow_dispatch:
  schedule: # Run every day at midnight (UTC)
    - cron: '0 0 * * *'
  push:
    branches:
      - main
      - develop
    paths-ignore:
      - '**.md'
      - 'docs/**'
  pull_request:
    branches:
      - main
      - develop
    paths-ignore:
      - '**.md'
      - 'docs/**'

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Setup Haskell
        uses: haskell-actions/setup@v2
        with:
          ghc-version: '9.6.7'
          enable-stack: true
          stack-version: 'latest'

      - name: Cache Stack
        uses: actions/cache@v4
        with:
          path: |
            ~/.stack
            .stack-work
          key: ${{ runner.os }}-stack-${{ hashFiles('stack.yaml', 'stack.yaml.lock', 'package.yaml') }}
          restore-keys: |
            ${{ runner.os }}-stack-

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libsdl2-dev

      - name: Install tools
        run: stack install hpc-codecov

      - name: Cache downloaded test ROMs
        uses: actions/cache@v4
        with:
          path: test/testroms
          key: ${{ runner.os }}-testroms-${{ hashFiles('Makefile') }}
          restore-keys: |
            ${{ runner.os }}-testroms-

      - name: Fetch mooneye and acid2 test ROMs
        continue-on-error: true
        run: make test-roms

      - name: Build project
        run: make build

      - name: Run tests with coverage
        env:
          OCELOT_GOLDEN: '1'
        run: stack test --coverage

      - name: Generate coverage report
        run: stack exec -- hpc-codecov stack:ocelot-test -o codecov.json

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: codecov.json
          fail_ci_if_error: false
